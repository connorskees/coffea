public class arithmetic {  
    public void main(String[] args) {
        int x = 2;
        int y = 3;
        
        int z = (x + y) * x - y;
        int b = x % y;
        int c = x << y;
        double a = Math.pow(x, y);
    }
}